//for in loop
let arr = ["aman", "anuj", "anil", "ram"];

// it will print key (0,1,2,3) insted of actual value
// by default key array elements is there index number
for (let val in arr) {
  console.log(val);
}
