//for in loop
let arr = ["aman", "anuj", "anil", "ram"];

// it will print key (0,1,2,3) instead of actual value
// by default key of array elements is their index number
for (let val in arr) {
  console.log(val);
}
